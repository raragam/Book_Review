  <div class="col-md-8 offset-md-1">

    <h2>会員一覧</h2>

  <table class="table">

    <thead>
      <tr>
        <th>登録画像</th>
        <th>登録名</th>
        <th>フォロー数</th>
        <th>フォロワー数</th>
        <th></th>
      </tr>
    </thead>

    <% @users.each do |user| %>

      <tbody>
        <tr>
      <td>
        <%= image_tag user.get_profile_image %>
      </td>
      <td>
        <%= user.name %>
      </td>
      <td><%= user.followings.count %></td>
        <td><%= user.followers.count %></td>
      <td>
        <%= link_to "詳細ページ", admins_user_path(user.id) %>
      </td>
        </tr>
      </tbody>

    <% end %>

   </table>

  </div>