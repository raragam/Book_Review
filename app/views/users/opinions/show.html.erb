

  <% if flash[:notice] %>
    <div class="flash">
      <%= flash[:notice] %>
    </div>
    <% end %>

    <div class="container px-5 px-sm-0">

    <div class="row">
      <div class="col-md-3">

  <h1>User info</h1>
 <%= image_tag @user.get_profile_image %>

 <table class="table">
   <tbody>
    <tr></tr>
    <tr>
    <th>name</th>
    <th><%= @user.name %></th>
    </tr>
    <tr>
    <th>introduction</th>
    <th><%= @user.introduction %></th>
    </tr>
   </tbody>
 </table>


   <% if @user == current_user %>

  <div class="row">
  <%= link_to edit_users_user_path(@user), class: 'btn btn-outline-secondary btn-block' do %><i class="fas fa-user-cog"></i><% end %>
  </div>

   <% else %>

   <div class="row">
  <%= link_to edit_users_user_path(@user), class: 'btn btn-outline-secondary btn-block' do %><i class="fas fa-user-cog"></i><% end %>
  </div>

   <% end %>


<h2 class="mt-3">New book</h2>
  <%= form_with model: @opinion_new, url: '/users/opinions', method: :post do |f| %>
  <div class="form-group">
    <div>
      <label for="opinion_title">Title</label>
    </div>
    <div>
      <%= f.text_field :title, class: 'form-control opinion_title' %>
    </div>
  </div>

  <div class="form-group">
    <div>
      <label for="book_title">Opinion</label>
    </div>
    <div>
      <%= f.text_area :body, class: 'form-control opinion_body' %>
    </div>
  </div>

    <div class="form-group">

       <%= f.submit 'Create Book', class: 'btn btn-success' %>

    </div>

  <% end %>

  </div>

  <div class="col-md-8 offset-md-1">

    <h2>Book detail</h2>

      <table class="table table-hover table-inverse">

        <tbody>
        <tr>
      <td>
        <%= link_to users_user_path(@user.id) do %>
        <div>
          <%= image_tag @user.get_profile_image %>
        </div>
        <div>
          <%= @user.name %>
        </div>
        <% end %>

      </td>
      <td>
        <%= link_to users_opinion_path(@opinion.id) do %>
        <span><%= @opinion.opinion_title %></span>
        <% end %>
      </td>
      <td>
        <span><%= @opinion.opinion_body %></span>
      </td>

      　<% if @user == current_user %>

      <td>
        <%= link_to "Edit", edit_users_opinion_path(@opinion.id), class: 'btn btn-sm btn-success' %>
      </td>
      <td>
        <%= link_to "Destroy", users_opinion_path(@opinion.id), class: 'btn btn-sm btn-danger', method: :delete, "data-confirm" => "本当に消しますか？" %>
      </td>
      <% else %>
      <% end %>
        </tr>
      </tbody>

      </table>

  </div>

  </div>

  </div>

