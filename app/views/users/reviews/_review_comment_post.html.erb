  <%= form_with(model:[review, review_comment], local: false, url: '/reviews/'+"#{review.id}"+'/review_comments', method: :post) do |f| %>
    <%= f.text_area :review_comment, size: "78x5", class:"mt-3" %><br>
    <%= f.submit '送信', class: "btn btn-primary btn-block mt-2" %>
  <% end %>