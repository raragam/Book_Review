  <table class="table">

    <thead>
      <tr>
        <th>登録画像</th>
        <th>登録名</th>
        <th></th>
        <th>フォロー数</th>
        <th>フォロワー数</th>
        <th></th>
      </tr>
    </thead>

    <% @users.each do |user| %>

      <%# if user.is_deleted == false %>

    <tbody>
      <tr>
        <td><%= image_tag user.get_profile_image %></td>
        <td><%= user.name %></td>
        <td><%= render "users/relationships/btn", user: user %></td>
        <td><%= user.followings.count %></td>
        <td><%= user.followers.count %></td>
        <td><%= link_to "詳細ページ", users_user_path(user.id) %></td>
      </tr>
    </tbody>

      <%# end %>

    <% end %>

   </table>